__pycache__/
*.pyc
.env

# User-generated media — never commit uploads/exports/test artifacts
static/uploads/
static/outputs/
static/promptbeta/
code_backup_*/

# Logs, temp and backup files
*.log
*.tmp
*.bak
static/processed/

# Live stats database — tracked by git previously, but deploys would
# clobber the server's accumulated signups/exports. Keep it local only.
stats.db
